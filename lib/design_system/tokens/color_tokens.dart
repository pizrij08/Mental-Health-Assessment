import 'package:flutter/material.dart';

/// Shared color palette inspired by the Lanserhof aesthetic described
/// in the product brief. Keeping these in one place makes it easy to
/// reuse colors across sections and adjust the look later.
class MindWellColors {
  static const cream = Color(0xFFF5F3EF);
  static const darkGray = Color(0xFF3F4A3E);
  static const lightGreen = Color(0xFF8FA88D);
  static const warmGray = Color(0xFF6D716A);
  static const surface = Color(0xFFFAFAF8);

  const MindWellColors._();
}
